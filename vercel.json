{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "vite",
  "outputDirectory": "dist",
  "buildCommand": "python3 -m pip install -r requirements.txt && python3 generate_data.py && npm run build"
}
