# ---- dependencies ----
node_modules/

# ---- build output ----
dist/
.vercel/

# ---- generated synthetic data ----
# The source dataset (data/) IS committed so the deploy is a plain Vite build
# (no Python at deploy time). It's also mirrored on Kaggle (see README).
# public/data/ is a build artifact recreated by scripts/prepare-data.mjs.
/public/data/

# ---- python ----
__pycache__/
*.pyc
.venv/
venv/

# ---- logs / OS junk ----
*.log
npm-debug.log*
.DS_Store
Thumbs.db

# ---- secrets / env ----
.env
.env.*
!.env.example
